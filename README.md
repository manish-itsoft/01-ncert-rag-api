# 02-ncert-rag-api
This repository implements the API layer for the NCERT RAG system using FastAPI. It handles semantic search over the vector database, prompt construction, LLM integration, caching, and standardized response formatting.
